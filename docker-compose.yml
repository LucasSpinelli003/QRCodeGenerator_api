version: '3'

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 3000:5432
    environment:
      - POSTGRESQL_USERNAME=qrcode
      - POSTGRESQL_PASSWORD=qrcode
      - POSTGRESQL_DATABASE=qrcodeGenerator